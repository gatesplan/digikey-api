# Constants
DigiKey Products V4 API 상수 모듈. 외부 의존성 없음 (Tier 0).

## Endpoints
API 기본 URL, OAuth URL, 각 엔드포인트 경로 상수 정의.
`{product_number}`, `{category_id}` 등의 placeholder 포함.

## Headers
DigiKey API 요청에 사용되는 커스텀 HTTP 헤더 이름 상수.
모든 헤더는 `X-DIGIKEY-` 접두사.
